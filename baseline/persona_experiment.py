from __future__ import annotations


def main() -> None:
    raise RuntimeError(
        "persona_experiment.py is intentionally disabled during the controlled tool-use pilot. "
        "Persona-conditioned runs remain deferred until the core environment and MI sanity suite are validated."
    )


if __name__ == "__main__":
    main()